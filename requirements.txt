# 易班自动签到依赖
requests>=2.31.0
pycryptodome>=3.19.0
js2py>=0.74
# 可选：YIBAN_PROXY 使用 socks5 代理时需要
PySocks>=1.7.1
# TUI 配置工具（服务器端 python3 -m tui 使用）
textual>=8.0.0
# TUI 实时性能监控（btop 简化版，M 键打开）
psutil>=5.9.0
