# 易班自动签到依赖
# 版本下限依据（批次14 C5 + 批次15 实施）：
#   requests>=2.34.2   CVE-2024-35195（代理凭证泄露）
#   urllib3>=2.7.0     CVE-2024-37891（代理走私）修于 2.2.2；CVE-2025-66471/66418（HIGH）
#                      修于 2.6.0；CVE-2026-21441（HIGH）修于 2.6.3——下限直接抬到
#                      当前 lock 版本，任何新部署从第一天起不低于安全修复线
#   pycryptodome>=3.19.1  CVE-2023-52323（RSA OAEP 时序侧信道）
#   flask>=3.1.3       CVE-2025-47278 修于 3.1.1、CVE-2026-27205 修于 3.1.3
#   werkzeug>=3.1.8    CVE-2025-66221/2026-21860/2026-27199（safe_join Windows 设备名）
#                      修于 3.1.4/3.1.5/3.1.6，下限抬到当前 lock 3.1.8
#   click>=8.5.0       CVE-2026-7246（传递依赖，此前下限无约束）
#   jinja2>=3.1.6      CVE-2025-27516（传递依赖，此前下限无约束）
#   idna>=3.15         CVE-2026-45409（传递依赖，此前下限无约束）
#   pygments>=2.20.0   CVE-2026-4539（传递依赖，此前下限无约束）
#   js2py 已移除：CVE-2024-28397 无修复版本，代码已不再依赖
#
# 注意：CI 与 Docker 镜像按 requirements.lock（精确锁定）安装；requirements.txt 的
# 下限用于裸机部署（README/部署清单），必须不低于各 CVE 修复版本且不高于 lock，
# 保证「被审计的产物 = 被部署的产物」（批次14 P2-3）。
requests>=2.34.2
pycryptodome>=3.19.1
urllib3>=2.7.0
# 可选：YIBAN_PROXY 使用 socks5 代理时需要
PySocks>=1.7.1
# TUI 配置工具（服务器端 python3 -m tui 使用）
textual>=8.0.0
# 网页管理系统（web 模块）
flask>=3.1.3
werkzeug>=3.1.8
click>=8.5.0
jinja2>=3.1.6
idna>=3.15
pygments>=2.20.0
# 生产常驻服务（web/deploy/yiban-web.service 与部署清单用 gunicorn 启动，
# werkzeug dev server 禁止公网直连）
#   gunicorn>=23.0.0   CVE-2024-6827（HTTP request smuggling，22.0 及以前受影响）
gunicorn>=23.0.0
