# 易班自动签到依赖
# 版本下限依据：
#   requests>=2.34.2   CVE-2024-35195（代理凭证泄露）
#   urllib3>=2.2.2     CVE-2024-37891（代理走私）——requests 的传递依赖，显式声明
#   pycryptodome>=3.19.1  CVE-2023-52323（RSA OAEP 时序侧信道）
#   flask>=3.1.0       Werkzeug 3.1.x 系列安全下限
#   werkzeug>=3.1.3    CVE-2024-49766/49767（dev server DoS）
#   js2py 已移除：CVE-2024-28397 无修复版本，代码已不再依赖
requests>=2.34.2
pycryptodome>=3.19.1
urllib3>=2.2.2
# 可选：YIBAN_PROXY 使用 socks5 代理时需要
PySocks>=1.7.1
# TUI 配置工具（服务器端 python3 -m tui 使用）
textual>=8.0.0
# 网页管理系统（web 模块）
flask>=3.1.0
werkzeug>=3.1.3
# 生产常驻服务（web/deploy/yiban-web.service 与部署清单用 gunicorn 启动，
# werkzeug dev server 禁止公网直连）
#   gunicorn>=26.2.0   CVE-2024-6827（HTTP request smuggling，22.0 及以前受影响）
gunicorn>=26.2.0
