# 更新日志

## [v0.4.5] - 2026-09-18 · 权限分档、取证闭环与会话安全判定

**新增**
- 被窃会话的追回：主管理员改口令后此前拿到的会话立即失效，不必再等它自己过期。
- 邮件正文引入排版层：同一份内容声明同时生成纯文本与网页两个部件，纯文本邮件里不再出现 HTML 实体或标签。
- 只读面（账号详情、用户、日志、签到事件）补聚合留痕与详情读取限速，被窃会话批量拖库会留下记录、也受次数约束。
- 审计清理量随每日告警与取证工具一起输出（累计有留痕的删除条数、最近一次清理的截止点）。服务器时钟被缓慢拨快时本机自检发现不了，这两个数字是给离机记录比对用的。
- 系统设置按影响半径分档：会造成全站停止签到的项（签到窗口、周末开关、容量上限、探针与账号验证、执行体与告警通道等）只有主管理员输入当次口令才能改，改动的前后值进审计；日常调度项仍由任意管理员调整。

**修复**
- 审计记录被删除无法发现：删掉最近若干条再用一条新写入掩盖、或整表清空，此前都能通过自检。现在库外锚点比对改为「定点 + 稠密 + 有留痕」三重判据，锚点文件自身也成链，物理删除逐次记账；取证工具一次跑齐链自洽、锚点比对与写入欠账三项。
- 口令复核口可被用来猜主口令：原先每秒可试约 6 次、十五分钟才告警一次。现在收成一个入口，带独立的失败计数与冷却，并与登录失败计数分开记账（不会把管理员锁在门外）。
- 周末开关与签到窗口这类「看起来是运营参数」的设置，原先任意管理员或持被窃会话者都能改，可造成全站静默漏签；现在归入受口令保护的一档。急停例外：暂停签到人人可做，恢复只有主管理员可做。
- 管理员改写他人易班密码或改绑手机号，现在需要当次口令，并会给当事人发一封变更通知（不受其个人通知开关影响）。
- 全站公告改为双人发布：普通管理员只能保存草稿，主管理员输入口令后才对外可见；清空草稿再发布即下线，撤下错误公告不必再登录服务器改配置。
- 服务器时区不再影响清理与超龄判断：审计记录保留期、校验任务收口、通知额度重置统一按北京时间，UTC 服务器上不再多滞留 8 小时。
- 会话 Cookie 的 Secure 判定只在第一跳是可信任的反向代理时才采信转发头。此前客户端自己发一个请求头就能让本站 Cookie 一直要求 HTTPS，站点一旦退回 HTTP 就登录不上；现在按每个请求判定，代理头消失也能回退。
- 通知与邮件配置：SMTP 目标默认不允许指向内网或保留地址（本机或内网邮件服务器需在配置里显式开启）；地址长度与条目数量设了上限；密钥校验按保存后实际生效的类型判定，此前「不带类型、只提交密钥」可绕开地址白名单。
- 邮件发送失败的日志不再记录异常类型名——类型名互不相同，等于把目标端口是否开放的探测结果写进管理员可读日志。
- 日志与审计的凭据脱敏补齐：设备凭据正则不再截断含右括号的密码；访问令牌、会话标识与密钥字面量、收件人列表、出站地址段都会打码。
- 登录流程与协议层收紧：已登录判定只认可信站点地址，重定向逐跳校验，防止恶意站点伪造登录态或把请求带到站外。
- 窗口关闭后才起跑的一轮零请求却报「失败」并触发失败提醒的问题已修（现在如实记为跳过）；并发执行体先写下的结论不会被覆盖；汇总邮件异常不再影响退出码。

**变更**
- 注销记录的 IP 哈希换了算法（与全站 IP 匿名口径统一）。按 IP 判定的注销冷却对升级前的存量记录会重算一次，属预期行为。
- 账号数据与系统配置无需迁移，升级后直接使用。

## [Unreleased] - 文档与代码结构调整

- 项目介绍恢复为功能特性清单，删去「以下是界面上看不出来、但会影响你部署决策的事实」这类部署手册式引导句，介绍回归「它是什么、能做什么」的本意；相关内容均已在正文对应章节保留。

**变更**
- 数据库层按职责拆分：连接、审计链、事件统计、迁移、用户、每日清理各成一处，行为、数据与配置不变，账号数据无需迁移。
- 网页后端按功能域拆分：全部页面与接口归入独立模块，地址、权限与行为不变。
- 日志通道名细分：上述模块的日志从统一的 `yiban.db` 改为各自的模块名（如 `yiban.store.migrations`），日志内容与格式不变；按旧通道名过滤日志的部署需要同步调整过滤条件。

## [v0.4.4] - 2026-09-16 · 登录流程与通知模块整理

**变更**
- 登录与签到流程按职责拆开：易班协议步骤、安全校验（风控拦截判定与跳转白名单）、会话与凭据管理各成一处，行为与配置不变；后续跟随易班改版调整时不必再在长文件里翻找。
- 通知推送与告警邮件同样拆为独立模块，发送成功率与每日额度的口径保持不变。
- 登录失败时的诊断日志补充响应形状（最终地址、状态码、长度、特征命中数），排查风控拦截与页面改版更快；地址中的查询参数与凭据一律打码。
- 新增一张签到分工记录表（首次启动自动创建）：为多执行体并行签到做准备，账号会按"领取"方式分给执行体，同一账号不会被两个执行体同时登录。账号数据与系统配置无需处理。

## [v0.4.3] - 2026-09-16 · 容量口径与数据恢复修正

**修复**
- 账号容量不再把未通过审核的账号计入，新账号不会因「名额已满」被误拒；总览的容量说明同时标出这部分账号的数量。
- 审核通过时会检查账号容量，避免审批后无声超出上限。
- 先删掉账号再注销的用户，在有效期内恢复时账号会一并带回（原先恢复后可能一个账号都没有）。
- 补全按日状态文件的清理规则，状态目录不再逐日累积、签到日历的按天查询不再随之变慢。
- 容器重启后不再重复触发一轮全站签到。
- 签到异常告警的判断时刻与补签时刻对齐，不再误发或漏报。

**变更**
- 每日清理改为宿主与容器共用一套规则，新增按日文件会被检查拦下。
- 账号数据与系统配置无需迁移，升级后直接使用。

## [v0.4.2] - 2026-09-15 · 签到可靠性修复

**修复**
- 服务器时区设置导致当天全部账号不签到：签到窗口与「今天」的判断统一按北京时间，服务器设在任何时区结果一致。
- 删除账号后仍被签到：每次执行前复核账号有效性，已删除或被打回的直接跳过，不发起请求。
- 在线校验任务卡住导致新增账号校验全部失败：超时任务自动收口并释放待办名额。
- 管理员审核结果被后台校验覆盖：审核通过后不再被迟到的校验结果改回「已拒绝」。
- 删除账号时未清理其校验记录；补签轮把上一轮的「失败」改写成「窗口外」，失败提醒随之漏发。
- 窗口前后留白被设得过大时「有计划却一个都不执行」「容量显示为 0」的自相矛盾。
- 签到进程起跑较晚时容量预估偏乐观：改按剩余时间估算并在超出时提醒。
- 「我的账号」页的删除、暂停、编辑可能作用到另一个账号上：列表变化时提示刷新后重试。
- 失败提醒邮件的每日额度被一次发送故障占用：改为发送成功才计入额度。
- 同一手机号在账号配置里重复出现导致重复签到（现按号码去重并告警）。
- Docker 部署不可用：镜像漏打包共享代码导致容器启动失败；国内构建镜像可指定软件源。

**变更**
- 模拟客户端上报的版本特征同步至商店最新版，降低与服务端版本校验不一致的风险。
- 补签轮不再对「今天没任务」的账号重复执行一次完整登录。
- 账号数据与系统配置无需迁移，升级后直接使用。

## [v0.4.1] - 2026-09-14 · 运营面收口、容量口径统一与显示修正

**新增**
- 页面访问错误的友好处理：不存在的地址与服务器错误不再返回空白页，改为与站点一致的提示页，并按当前身份给出返回入口（未登录回登录页，登录后回总览或签到日历）。
- 新增爬虫协议文件：仅放行登录页与静态资源，登录后的管理页面一律不允许被搜索引擎收录。
- 补全站标族：iOS「添加到主屏幕」图标复用站点图标；《用户协议》《隐私政策》独立页此前不显示标签页图标，现已与主站一致。

**变更**
- 容量估算口径统一：容量提示与服务端保存校验此前各用一套算法与阈值，现在同源，估算更接近实际承载能力——同样配置下可容纳的账号数由 260 上调为 360，介于两者之间的站点调整账号间隔不再被误拒。单账号耗时默认值改为按实测标定的 3 秒（真实网络更慢时可在配置中覆盖）。
- 密钥轮换的停服检查与提示语行为一致：确认进程无关后使用强制参数可正常继续，此前按提示操作仍会被拒绝（该检查不读取强制参数）。
- 超宽屏（2K/4K）信息密度提升：热力图随视口放大格子，减少卡片内大片留白；协议与隐私模态正文行宽收窄到约 40 字/行，长文不再跳行。

**修复**
- 修复数据总览「今日签到成功率」数字偶尔显示为灰色的缺陷：页面两处数据请求先后到达时，该卡可能保留「今日无记录」的空态样式，导致真实数值以灰色显示。
- 修复上述空态下「—」占位被渲染为超大横杠的问题。
- 中文界面上的中等字重（500/600）统一改为存在字面的字重（中文只有常规与粗体两档），避免浏览器合成造成的模糊与偏细。
- 修复 CI 门禁：代码风格检查此前长期失败并连带跳过测试步骤，现已全部通过。

**升级说明**
- 本次为界面与口径修正，账号数据与系统配置无需迁移，升级后直接使用。

## [v0.4.0] - 2026-09-13 · 界面整体重写

**新增**
- 管理端与用户端界面整体重写：导航按「数据 / 工作台 / 我的」分组，页面路径统一为分组形式，账号、用户、日志等列表页改为标签页切换，常用视图一屏可达。
- 新增数据总览页：关键指标卡片与签到热力图并排展示，容量占用与当日签到情况一目了然，卡片可整卡点击跳转到对应页面。
- 新增自研日期、时间与下拉选择控件：统一交互与外观，日期可直接输入或弹层选择，深色模式下同样可用。
- 页面补齐加载与失败状态：数据加载中显示指示，接口失败给出原因说明与「重试」入口，不再出现空白页。
- 支持键盘操作与读屏：导航、标签页、弹窗与表单补齐焦点顺序和语义标注，主要操作可全程用键盘完成。
- 暗色主题完善：深色模式下配色、对比度与图标显示统一，主按钮与次要文字对比度达到可读标准。

**变更**
- 用户端首页改为签到日历，登录后先看到当日签到结果。
- 设置页按功能重新分区，各分区独立保存并明确提示未保存改动；弹窗类操作统一为显式保存。
- 公告改为顶栏通知入口，页脚与侧栏重复的版本、开源入口合并。
- 统一全站分隔线、卡片标题与表单排版规范，优化中文小字的行高、字重与标点排版。
- 动效统一：入场节奏、切换过渡与弹层动画方向一致，并尊重系统「减少动态效果」设置。
- 本次为界面整体重写，账号数据与系统配置无需迁移，升级后直接使用。

**优化**
- 全站文案精简与校对：说明更简短直接，错误提示给出下一步操作。
- 移动端与横竖屏适配：小屏隐藏次要列、操作列固定显示，弹窗按钮常驻可点，避免遮挡与溢出。
- 表单输入体验优化：补充密码可见性切换、设备信息填写帮助与必填格式提示。
- 精简静态资源引用，移除自托管字体与旧样式栈。

**安全**
- 系统级开关（如暂停签到、关闭推送等）变更改为服务端真实校验操作口令，口令错误时在弹窗内提示并可重试。
- 用户操作结果中的邮箱地址统一脱敏显示。

**修复**
- 修复旧版页面在部分缩放比例与窄屏下的布局错位、按钮换行与弹窗越界问题。
- 修复日期弹层切换月份无效、日历切换跳变与默认选中不准确的问题。
- 修复标签页深链在刷新后未回到对应页面的问题。

## [v0.3.4] - 2026-09-10 · 安全加固、补签修正与容量明细

**安全**
- 修复一处配置注入提权漏洞：普通管理员可借公告等写入通道注入隐藏配置行、覆盖主管理员凭据完成提权。现写入与解析使用同一行边界判定，主管理员凭据异常时拒绝登录并即时告警，启动时自动检测存量可疑配置行。
- 修复密钥轮换遗漏迁移邮件发信配置的问题：更换密钥后告警邮件不再静默失效，轮换自检覆盖全部告警通道。
- 日志导出改为服务端脱敏副本并纳入审计与限速：导出内容与页面查看口径一致（手机号打码），导出行为全程留痕。
- 修正告警通道可用性判定：配置存在但已失效时不再误判可用导致告警哑火；汇总通知在邮件收件人为空时自动改走消息推送兜底。
- 修复备案信息链接未校验协议的问题：仅放行网页与邮件类链接，杜绝脚本类注入公开页面。
- 前端加固：动态生成的行内事件与脚本上下文统一安全转义，消除属性注入与脚本破坏隐患。
- 通道健康日报附带审计链锚点（链首标识与记录条数），便于离机核对审计记录完整性。
- 修改告警收件人纳入高危操作：需主管理员密码确认，原收件人会收到变更通知，防止告警接收方被静默篡改。
- 删除账号（含批量删除）纳入高危操作限速并即时通知：短时间内反复删除会被暂时拒绝，每次删除都会提醒管理员；被删除账号仍在保留期内可恢复。

**新增**
- 系统设置新增「用户容量上限」「账号容量上限」：仅主管理员可调，0 为不限；调小不会删除现有用户或账号，仅限制新增。
- 系统设置「消息推送」卡新增告警收件人配置：可填写一个或多个邮箱，无需再登录服务器修改配置文件。
- 登录页与协议页补充站点简介，分享链接与搜索结果不再只显示标题。
- 系统设置容量卡片新增账号名额占用明细：按「正常 / 用户自暂停 / 账密故障暂停」分类展示，便于判断名额紧张时是否需要清理长期停签账号（停签账号同样占用名额）。

**变更**
- 容量口径统一：签到容量按有效签到窗口（扣除首尾保护段）与账号间隔统一估算，占用按实际参与签到的活跃账号数计算（含管理员直属账号）；概览卡新增「已注册未提交（潜在负载）」提示，超容量仅警示不阻断。
- 补签改为定向重跑：仅重跑当日未完成的账号，已完成账号不再重复登录，降低重复请求。
- 探针纳入暂停与周末开关：一键暂停或关闭周末签到期间，健康探针一并停止。
- 账号间隔明确为下限语义：两次尝试间隔不小于设定值，与自动调度口径一致。
- 账号编辑仅在实际更换凭据（密码/手机号）时重置熔断计数，修改备注不再清零连续失败天数。
- 修改容量上限、账号间隔等敏感设置需再次输入管理员密码确认。

**优化**
- 统一表单占位提示与已填内容的文字大小：输入框、下拉框与容量上限输入框字号一致，同一行控件不再大小不齐。
- 签到重试日志补充失败原因：账号进入重试时，日志与账号状态直接给出本次失败原因，排查不必再翻事件记录。

**修复**
- 修复用户提交账号校验失败（如账号或密码错误）时，错误提示被提交弹窗遮挡不可见的问题：失败原因改为直接显示在弹窗内部，无需关闭弹窗即可看到，已填内容不丢失。
- 手动签到子进程异常退出（如签到队列忙）不再误报成功，真实原因写入签到日志。
- 签到轮被超时终止时，已收集的告警汇总先行发出，不再随进程丢失。
- 熔断暂停或用户自取消的账号不再占用请求间隔，避免失联账号把正常账号挤出签到窗口。
- 服务器重启后同一签到时段不再重复触发全量补跑，当日已完成账号的状态不再被覆盖。
- 机器错过早班、越过硬补签点后以首签身份收尾时，「部分成功+窗口外」不再漏发告警。
- 修复宿主定时任务与网页对配置文件解析不一致的问题（特殊字符导致配置分叉）。
- 邮件通道健康检测区分「未配置」与「损坏」，清空的配置不再被误诊为密钥失配。
- 通知通道配置保存恢复「先校验、后落盘、再告警」顺序，落盘失败不再收到误导性变更通知。
- 备份强制加密选项与异机副本配置解耦：单机部署同样可强制本地加密归档，加密失败仍不保留明文。
- 邮箱打码收紧：短账号名只保留首位，避免近乎全量暴露。
- 修复注销账号后恢复时自选签到时间片丢失的问题：两种删除方式对自选时间片的处理不再不同，恢复后自选完整保留。
- 修复补签长期未实际执行的问题：首签流程结束后立即判定当日未完成账号并补跑，失败账号当天即可获得第二次尝试，不再等到次日。

## [v0.3.3] - 2026-09-07 · 移除终端配置面板，统一网页管理

**变更**
- 系统设置页重组为六张卡片：容量预估并入概览、「移动端完整表格」开关并入批量操作、周末签到与账号间隔并入签到调度、服务器连通性检测并入健康检查、邮件通知并入消息推送（SMTP 发件邮箱改为列表配置，支持主备多个，可在网页编辑）、修改主管理员密码并入危险区。
- 启动延迟废弃：错峰打散由签到调度承担，`.env` 的 `YIBAN_START_DELAY_MAX` 读取后不再生效。
- 账号间隔默认 10 秒，自动与手动签到均生效，可在「系统设置 → 签到调度」调整（修改需主管理员密码确认）。
- 移除服务器终端配置面板（TUI）：账号录入、排序与账号间隔等配置统一改用网页管理后台，功能完全覆盖原面板，升级后请以网页后台完成配置。
- 「周六签到」默认改为关闭，与周日开关语义一致：升级后周六默认不签到，需要的请在「系统设置 → 周末签到」开启；已显式调整过周六开关的部署不受影响。

**新增**
- 签到日志支持按关键字检索、一键查看当日全部日志，并可按日期导出完整日志文件。
- 系统设置新增「容量预估」（概览卡）：按签到窗口与账号间隔估算可容纳的账号数与用户数，占用超过 80% 时标红提醒，超容量后保存新的账号间隔将被拒绝。
- 账号审核被拒绝时，提交者收到附拒绝理由的邮件通知；批量拒绝同样逐人通知，审核通过不另发。

**优化**
- 待处理账号列表重排：待审核置顶、最新提交最前，已拒绝账号沉底。
- 注册页密码要求改为在输入框下方完整展示，手机端不再显示不全；账号表单在手机端使用更大输入字号，聚焦时不再触发页面缩放。
- 账号备注示例改为「专业 + 班级 + 姓名」格式，不再建议网名或代号；设备型号与识别码改为按易班 App「设备绑定」页面显示的信息原样填写。
- 界面细节优化：图形开关增加键盘焦点指示、长邮箱在表格与顶栏截断显示（悬停可看全文）、说明浮层增加淡入淡出过渡、移动端「完整表格」开关补齐隐藏列，其说明文案精简并移入悬停提示。
- 消息推送卡与「ⓘ」说明浮层排版优化：长说明收纳进「ⓘ」浮层并改为分点「标签一行 + 说明一行」结构，邮件通知区提示同步收纳；全站卡片标题与表单标签字重统一加粗。

**修复**
- 修复仅有已拒绝账号的用户未出现在用户管理「待处理用户」栏的问题：该栏与「账号管理 → 待处理账号」同口径（待审核 + 已拒绝）。
- 修复手机端登录页公告文字被右上角主题切换按钮遮挡的问题。
- 修复电脑端放大网页（如 130%）后，账号添加/编辑与账号提交弹窗的确认按钮可能被移出可视区、点击无效的问题：弹窗改为「内容区滚动 + 按钮固定底部」，任何缩放比例下按钮均完整可见、可点击。
- 修复弹窗打开期间背景页面仍会被滚动的问题：所有弹窗打开时背景不再跟随滚轮或触摸移动，关闭后恢复正常滚动。
- 修复「ⓘ」说明浮层在页面右侧展开时超出屏幕无法阅读、配色与主题颠倒的问题：浮层贴齐图标右缘展开，浅色模式白底深字、深色模式黑底浅字。
- 修复窄屏下「保存额度」等按钮文字竖排换行、批量操作等开关被长文案挤到下一行的问题：开关固定右缘对齐；SMTP 条目编辑器各输入框统一对齐，「掐头去尾」前后标注移至输入框下方。
- 修复邮件通知「未开启」提示语与实际不符的问题：改为引导主管理员在页内完成配置。

## [v0.3.2] - 2026-09-06 · 攻击面收口与告警可靠性

**安全**
- 修复「服务条款」「隐私政策」页面的脚本注入漏洞：特制链接中的恶意内容不再可能以网站身份在访客浏览器中执行，杜绝借此冒用登录身份。
- 调整消息推送的冷却时间或每日额度参数，需重新输入管理员密码确认；推送与邮件通道的变更提醒改为先送达后生效，不再可能被刚调整的参数静默吞掉。
- 「清空用户账号」操作与「完全删除用户」同口径：需重新输入管理员密码确认、纳入频率限制并触发重要告警。
- 编辑已生效账号并改绑手机号时，一律回到待审核状态重新确认，不再直接生效（仅修改密码或设备信息不受影响）。
- 修复账号处于待审核或已删除状态时，其手机号对应的历史签到记录与日志仍可被查看的越权问题。
- 注册页对「已注册」与「注销冷静期」统一提示，不再可被用于探测近期注销的账号。
- 子路径部署形态下，登录会话的生效范围收窄至应用自身路径，同域其他应用不再可能收到会话凭据。
- 登录失败提醒改用独立的通知额度，不再挤占重要告警的每日推送预留。

**变更**
- 单条手动签到与批量签到共用同一冷却保护：任一方式触发后，冷却窗口内再次手动签到将被拒绝（窗口时长可调整）。
- 批量手动签到单次上限与其他批量操作对齐（10 条），执行时限随账号数量自动放宽。

**修复**
- 批量手动签到超时不再被静默中断：未执行的账号不再凭空丢失，日志也不再误报「已完成」。
- 首次签到未实际执行（被跳过或服务器重启错过）时，补签轮不再被误判为首发轮，「部分成功 + 窗口外」的当日异常告警恢复送达。
- 当日全员窗口外或零成功时，邮件通道不可用自动转手机推送兜底；收件人未配置时明确留痕提示。
- 修复运行日志每条重复记录两次的问题。
- 签到执行时限配置增加下限与格式校验，误配置不再导致当天静默停摆或失去超时保护。
- 修复签到窗口起止被前后裁剪设置挤空时静默回退默认窗口、配置与实际不符且无提醒的问题。

**优化**
- 精简全站提示文案：登录页改为引导「先注册再登录」，注册示例邮箱改用占位域名，去除冗余说明。
- 内存态限速与锁定记录定期清理并限制长度，避免长期运行不断累积。
- 配置写入过程默认以私密权限落盘。
- 清理无引用的历史代码与过期文档描述；修复代码镜像同步的工作流配置错误。

## [v0.3.1] - 2026-09-01 · 注册管控与告警体系加固

**新增**
- 管理员可一键暂停新用户注册：暂停期间登录页关闭注册入口并明示原因，已注册用户登录不受影响；开关位于设置页危险区，仅主管理员可操作。全新部署默认暂停注册，完成初始配置后开启；既有部署升级后行为不变。
- 用户提交易班账号后，管理员即时收到「新账号申请待审核」提醒。
- 签到尝试全程留痕：日志页新增「签到事件」时间线，记录每次签到、重试与跳过，支持按账号与时间范围查询；当日全员无签到结果时发送专项告警。
- 新增账号加密密钥的停服轮换能力，全程防半完成状态与密钥丢失。
- 消息推送额度拆分为「日常」与「重要」两档：重要告警独立计数（默认每日 3 条，可调整），不再被日常告警挤占；额度用尽时补发邮件告知，避免静默期。

**变更**
- 调度器超时配置统一以配置文件为准，避免容器部署时网页设置不生效。
- 设置页容量口径调整：用户数含全部注册用户，账号数按实际持有账号的用户计，上限调整为 500 用户 / 200 账号。
- 关闭消息推送、更换推送密钥、关闭邮件通知等影响告警通道的操作，需重新输入管理员密码确认，变更后即时告知。
- 新增登录、退出与恢复登录的审计记录，便于核查异常会话。
- 系统时钟跳变导致清理任务被拦截时，发送邮件告警并提供人工重置工具，不再无限期静默冻结。

**优化**
- 后台运行日志并入按天签到日志统一留存，日志页同步展示，便于排查。
- 探针改为指定时间后周期性尝试并自动重试，未执行时不再逐次记录跳过日志。
- 密码错误触发的临时锁定不再推送手机，仅同一来源连续尝试多个账号时升级为重要告警。
- 登录页与账号表单说明调整：统一以注册邮箱登录；设备信息仅学校开启设备绑定时必填；账号备注建议填网名代号，不暴露真实姓名。
- 批量手动签到合并为单队列执行，汇总邮件精简为一封；失败重试精简为最多 3 次；易班未返回点位的账号不再反复重试，失败原因附服务端说明。
- 电脑端备案信息居中显示；密码强度提示统一口径并补齐前端校验。
- 测试用例按功能分类组织、支持并行执行，全量回归耗时缩短约三分之二。

**安全**
- 提交或修改签到账号时的密码验证新增防锁定保护：同一手机号短时间内多次密码错误将被临时限制验证并明确提示，防止反复试错触发易班账号锁定；验证未通过的提交写入防篡改审计日志（仅记失败类别）。
- 批量手动签到增加冷却保护（默认 30 分钟，可调整），防止被盗会话循环触发。
- 重置用户密码与彻底清除账号统一要求重新输入管理员密码确认，并纳入频率限制；彻底清除后发送重要告警。
- 修改用户管理员权限（设为 / 取消管理员）须重新输入当前管理员密码确认，并移除批量修改权限的入口：角色变更仅支持针对单个用户操作，防止被盗会话批量改动权限。
- Docker 加密备份修复产出空包的问题；口令不再暴露于命令行，解包前执行路径穿越等安全校验；加密失败即中止并告警，不再生成含全部密钥的明文归档。
- 修复裸机部署下审计锚点校验误报与失效的问题；数据导出过程中不再改写审计链。
- 密钥轮换增加防崩溃与防混合密钥强化；「最后一名注册管理员」的删除与降权保护下沉至数据库事务，消除并发竞态。
- 管理员口令为公开模板默认值或弱口令时，应用拒绝启动（fail-closed）。
- 主管理员口令策略提升为至少 12 位，且包含大写字母、小写字母、数字、符号中的至少三类：修改口令与部署 / 重置口令时的启动校验同一口径，不满足策略的弱口令拒绝启动；普通用户口令要求不变。
- 运行依赖与反向代理升级至安全维护版本。

**修复**
- 账号或密码错误属确定性失败，签到不再自动重试，避免重复的错误登录加速易班侧锁定；网络类失败重试策略不变。
- 修复消息推送每日限额在网页端与定时签到分别计数、实际发送可能超限的问题。
- 修复部分账号未完成时整批首签 / 补签被错误跳过的问题。
- 修复整表保存移除账号后，其签到历史中明文手机号残留的问题。
- 修复登录状态被服务端作废后沿用失效会话反复失败的问题：检测到即清除缓存并重新登录。
- 修复并发或批量操作可能改写到非目标账号的问题：目标已被变更时拒绝提交并提示刷新。
- 批量手动签到包含未配置账号时明确告警，不再静默忽略。
- 日志文件异常时降级为仅告警，不再影响网页服务运行。
- 修复部分环境下日志页按日期查看偶发显示错误的问题。

## [v0.3.0] - 2026-08-29 · 调度可靠性与账户安全加固

**新增**
- 注册增加「确认密码」输入框，密码框支持一键显示 / 隐藏明文。
- 注册与添加账号拦截临时邮箱与示例域名，减少垃圾账号。
- 新增「周末签到」设置：周日默认不签，周六默认照常签到，均可单独开关。
- 新增「消息推送」：支持 Server酱 或自定义通知地址，告警实时推送到手机，支持测试消息；同类型告警 60 秒内只推一条；可开启「仅推送重要告警」；每日最多 5 条（可调整），避免超出第三方免费额度，邮件不受影响。

**变更**
- 删除自己的易班账号后 7 天内可自助撤销，超期自动清除。
- 单次批量操作上限 10 条；随机延迟等高级调度设置仅主管理员可改；彻底清除宽限期注销账号仅主管理员可执行并触发告警。
- 普通管理员不再显示无权操作的消息推送与一键暂停卡片，调度配置仅可查看。

**安全**
- 退出登录或修改密码后，其他设备的登录状态立即失效。
- 外部通知地址仅允许 HTTPS，邮件发送通道启用证书校验。
- 改密、删号、权限变更等敏感操作触发安全邮件与告警；删除用户、彻底清除等敏感操作需重新输入管理员密码确认，并增加频率限制。
- 删除或变更最后一名主管理员前进行校验，避免系统锁死。
- 同类告警邮件默认 5 分钟内只发一封，防止异常反复触发耗尽发送额度；消息推送仍实时送达。

**修复**
- 修复消息推送开启后无法关闭的问题。
- 修复个别账号签到成功导致整批首签 / 补签被跳过的严重问题。
- 多任务签到改为随机选点、任一成功即完成，不再重复提交后续任务，降低请求量与风控风险。
- 定时签到进行中提交手动签到不再被静默丢弃，会提示稍后重试。
- 账号恢复健康后自动解除暂停；网络短暂失败不再延长冻结周期。
- 修复账号恢复后需重新登录及旧会话仍可用的问题。
- 修复系统时间被篡改可能导致审计记录被清除的问题；新增调度超时保护，避免连续超时导致漏签。
- 修复特殊字符绕过邮箱黑名单的漏洞；找回密码增加频率限制。
- 修复设置保存时随机延迟被意外清空及若干审计记录问题。
- 深色模式下「我的账号」状态图标与「按日期查看」日历图标显示正常；日志页「回到今天」不再误显示昨日日志。

## [v0.2.8] - 2026-08-27 · 综合安全体检与调度优化

**安全**
- 密码存储加固，历史弱密码自动重新加密。
- 同一账号同一天最多一封失败告警邮件（发送成功方计数），大批量告警自动汇总防拒收。
- 容器支持以非 root 低权限运行，提供数据与口令分离的加密备份脚本。

**变更**
- 容器模式下健康检查、周日签到、一键暂停等设置修改后即时生效。
- 邮箱端口配置错误时明确提示，不再静默回退默认端口；健康检查未开启时不再输出冗余日志。

**修复**
- 修复健康任务中断后未补充执行、空账号环境下健康检查报错的问题。
- 健康预警邮件表述修正：主题改为「易班账号健康预警」，正文说明为例行检查、不影响已完成签到。

**优化**
- 签到重试调度优化：失败账号当日窗口内智能重排、相邻请求保留最小间隔，临近截止直接给出明确结果。
- 连续失败暂停的账号定期探活，密码恢复后及时恢复签到。
- 大小写混合邮箱登录后个人设置正常保存；设置说明文案通俗化，部署文档勘误。

## [v0.2.7] - 2026-08-25 · 账号健康检查（探针模式）

**新增**
- 账号健康检查（探针模式）：管理员在「系统设置」开启后，按设定时间定期检测全部账号可用性，提前发现潜在签到失败。
- 异常账号邮件提醒：检查中发现问题账号时，管理员收到汇总提醒，对应用户收到自身账号提醒。
- 提交账号先验证：开启后用户提交账号即校验可用性，不通过当场提示原因。

**优化**
- 深色模式下探针模式触发时间与频率选项文字清晰可读。
- 浏览器标签页与收藏夹使用项目专属图标，替换默认图标。
- 页脚公安备案信息跳转官方查询页（含警徽标识），管理/会员/协议页完整显示。

## [v0.2.6] - 2026-08-24 · 邮箱通知

**新增**
- 配置发件邮箱后，签到失败、过慢、容量超载、登录连续失败等告警可同步发送管理员邮箱（与微信/推送互不干扰）。
- 管理员合并发送「易班签到汇总」邮件，替代逐条推送；用户自身账号最终失败时自动邮件提醒（成功不打扰，每日每账号最多 1 封）。
- 提醒可开关：普通用户在「我的账号」关闭失败邮件提醒（默认开启）；管理员在「系统设置」全局开关邮件。

## [v0.2.5] - 2026-08-23 · 容器化部署

**新增**
- Docker 容器化部署：Web 管理、安全访问、每日自动签到开箱即用；数据集中存放，升级、备份、迁移只处理一个目录。

**修复**
- 后台「用户/账号」容量统计不再计入已注销待清理账号；用户数按已提交易班账号统计，与实际一致。

## [v0.2.4] - 2026-08-17 · 合规与运营能力

**新增**
- 《用户协议》《隐私政策》合规文档接入：注册页强制勾选同意（后端校验），并提供独立文档页。
- 文档改为部署者填写模板，未填写时显示「尚未发布」占位提示。
- 全局一键暂停整站自动签到（当前执行中的跑完，手动签到不受影响），可随时恢复。
- 页脚备案信息展示区（运营者配置，未配置不显示）。

**修复**
- 合规文档在特定排版下渲染卡死的问题，并补充回归测试。
- 设置页全局公告卡片插入危险区卡片后垂直间距丢失的问题。

## [v0.2.3] - 2026-08-17 · 安全与数据架构升级

**安全**
- 集中修复数十项安全问题，覆盖数据读写、页面显示、服务配置与日志信息等层面。
- 修复多处权限与数据安全问题，包括越权提权、注销恢复异常、统计显示错误、已删除账号仍可操作等。
- 恢复已注销账号增加防暴力尝试保护。

**修复**
- 注销后可立即登录恢复，超期用户改为每日自动清除（与页面承诺一致）。
- 注销冷却期内登录给出明确提示与「恢复我的账号」入口；服务器时区在浏览器端显示一致；已注销邮箱添加账号绕过冷却期与人数上限的问题。

## [v0.2.2] - 2026-08-16 · 账号自助注销

**新增**
- 账号自助注销：分两次确认并验证密码，7 天冷却期内可用原邮箱与密码恢复（易班签到账号一并恢复，无需重新提交）；冷却期结束邮箱与账号释放。
- 管理员可查看已注销用户列表（邮箱/注销时间/剩余/状态），支持立即清除；后台显示当前登录账号名称（主管理员标注）。

**变更**
- 注销可撤销期由 3 天延长至 7 天，与易班账号保留期一致。
- 批量操作异常时整批撤回；强化「一人一号」约束；启动自动补齐数据库结构；管理操作记录增加防篡改保护。

## [v0.2.1] - 2026-08-15 · 签到调度与自选时间

**新增**
- 签到调度升级：多种时间分配方式（固定顺序/每天随机/高峰集中等），支持首尾时段缓冲与人数自动适配，改动需二次确认后保存。
- 自选签到时间：用户可自选 5 分钟时段，页面显示预计每日签到时间。
- 暂停自身签到（界面显示「已取消」，系统自动跳过）。
- 排队人数按当日实际执行顺序计算，暂停账号不占位。

**优化**
- 容量保护：注册用户数与账号总数设上限，超限拒绝并提醒管理员。
- 日志可按日期查看、历史记录可回溯近一年；设置修改留痕，服务端可追溯；页面缓存与加载性能优化；状态显示更稳定。

**修复**
- 修复特殊设置下调度功能报错、自选时间不生效、切换页面静默丢失调度设置、窗口末尾被提前中断导致漏签的问题。

## [v0.2.0] - 2026-08-14 · 自动错峰签到与状态提示重构

**新增**
- 自动错峰签到：每个账号每日在 06:30–07:50 窗口内分散分配时间点，到点自动执行；支持列表顺序/随机两种模式。

**变更**
- 签到状态显示体系重构：明确区分成功 / 跳过（未在执行窗口）/ 无需（学校无任务），管理端悬停可看原因，统计卡拆分为五类。
- 新增「周日签到」开关（默认关闭，需服务端开启）；用户页「最近签到记录」仅显示当日。
- 单次签到日志统一为 4 条（开始执行/登录成功/签到结果/汇总）。

**优化**
- 移动端与深色模式界面细节：表格圆角对齐、小屏次要列隐藏、操作列固定显示、切换淡入动效、字体策略（低清屏用系统字体）、全局圆角体系与密码提示文案统一。

## [v0.1.5] - 2026-08-13 · 数据存储迁移至数据库

**新增**
- 账号与用户数据迁移至数据库存储，支持多人并发不互相覆盖，手机号全局唯一。
- 关键管理操作（增删改/审核/批量/用户管理）自动留痕，服务端可查询追溯。
- 首次启动自动迁移旧数据并保留备份；备份同步升级为数据库 + 配置完整备份。

**修复**
- 提升数据读写稳定性；异常请求格式不再导致服务端内部错误；登录态传输增加加密保护；多人同时操作时列表不再短暂显示半成品状态。

## [v0.1.4] - 2026-08-13 · 凭据加密与备份升级

**安全**
- 易班账号密码与设备识别码改为加密保存，密钥首次自动生成，文件泄露无法直接读取。
- 新增一键备份工具（本地打包 + 异地加密副本 + 保留策略 + 恢复演练）；部署改为低权限运行、仅开放必要端口、加密访问。
- 清理历史仓库中残留的敏感信息。

**变更**
- 并发编辑保护：管理员同时编辑同一账号时，后保存方收到「已被其他管理员修改」提示。

## [v0.1.3] - 2026-08-13 · 安全加固与隐私保护

**安全**
- 全局隐私打码：账号列表、日志、日历等所有位置手机号/邮箱脱敏，完整信息仅操作时按需获取。
- 批量彻底删除增加保护，仅可删除已进入「待删除」列表的账号；主管理员改密后旧登录态立即失效。
- 登录增加防嵌入与安全防护；公告/拒绝理由等过滤异常字符并限长；添加账号不再生成临时密码，改为管理员填写初始密码。

**变更**
- 被拒绝账号不再自动签到（审核拒绝真实生效）；全账号跳过时区分「跳过」与「成功」。
- 密码策略升级：新设密码至少 10 位且含两类字符；登录页与后台增加防嵌入防护。

**新增**
- 签到模式切换：列表顺序 / 列表随机（每次打乱，时间更分散）。

## [v0.1.2] - 2026-08-12 · 列表与后台界面重构

**新增**
- 账号/用户列表支持人数统计、按名称/手机号/邮箱搜索，长列表限高且表头固定。
- 批量操作（系统设置开关启用）：多选后批量通过/拒绝/删除/设管理员/重置密码。
- 删除账号进入「待删除」列表，7 天内可恢复，超期自动彻底清除。
- 管理员权限分级：仅主管理员可设置/取消管理员；普通管理员保留其余管理权限。

**变更**
- 品牌字体与图标体系统一，建立全局圆角与配色规范。
- 账号列表、日志、日历等位置手机号/邮箱统一打码显示。

**修复**
- 修复批量操作报错、表格对齐/换行、勾选框位置、搜索框布局、表头单行显示等界面问题。

## [v0.1.1] - 2026-08-11 · 移动端适配与签到日历

**新增**
- 签到日历：按日期查看个人签到情况，颜色区分成功/失败/休息，点击查看当日明细。
- 自助修改登录密码。
- 移动端横向滚动表格适配，保持桌面列宽与清晰显示。

**优化**
- 改进平板与移动端页面滚动、布局稳定性；公告横幅固定显示、操作菜单（⋯）防误触、品牌图标与字体统一。

**修复**
- 修复管理端页面卡死、移动端无法滚动、竖屏遮挡等问题；提升日志查询与读取性能。

## [v0.1.0] - 2026-08-10 · 系统上线与账户基础体系

**新增**
- 网页版易班自动签到管理系统正式发布。
- 邮箱注册与登录，管理员后台审核用户提交的签到账号。
- 用户管理与账号管理界面，含待处理/待审核/正式等分组视图。
- 注册流程需勾选同意《用户协议》。
- 支持多位管理员，权限变更即时生效。
- 账号列表提交后自动刷新，同步最新审核状态。
