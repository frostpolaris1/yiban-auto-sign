# -*- coding: utf-8 -*-
"""基础设施层：与业务无关的通用能力（env 读写与锁、字段加密、跨进程锁原语）。

**依赖方向**：本层可被任何层导入，自己**不**导入业务模块（`yiban.store` /
`yiban.client` / `scripts.*` 等一律不引）。这样"加密/锁/env 解析"这类横切关注点
不会把业务逻辑卷进来，单独替换或测试都不需要拉起整个应用。

成员：`env_io`（.env 解析唯一实现）、`env_lock`（.env 写锁）、`account_crypto`
（AES-GCM 字段加密）、`locks`（跨进程锁原语，唯一实现）。
"""
