# 安全与告警瘦身计划（2026-09-23，v2 按用户取舍修订）

> **状态**：**已实施**（2026-09-24，分支 `feature/security-slim`）。实施记录、与计划书的有意偏离、以及登记项见 **§9**；任务书与派发纪律在 `.superpowers/sdd/security-slim/`（本地未入库）。
> 背景：防护体系按公网多租户 SaaS 标准建设，摩擦成本超过威胁收益。目标：**合理放宽安全验证、
> 缩减无关安全功能、减少告警邮件、简化功能体系**——砍防护过度，不砍真防护。
> **规模校准（用户 2026-09-23 确认）：百人以上真实多用户**（09-23 实查：账号 89 / 用户 110，
> 09-22 单日新注册 21，增长快）。这推翻了「小站可大胆删」的假设：**鉴权、IDOR 防护、隐私脱敏、
> 审计全部保留**，瘦身集中在「摩擦体验」与「告警噪音」两块。
> **核心设计输入（用户 2026-09-23）**：硬门禁尽量换**软性摩擦**——风控式加验、后台验证留痕、
> 加载动画/倒计时拉长操作时间来降低误操作风险，而不是要求输口令打断体验。

---

## 0. 分级原则与红线（v2 修订）

| 级 | 判据 | 处置 |
|----|------|------|
| **红线不碰** | 防真实攻击/数据丢失/隐私泄露 | 保留原样 |
| **软摩擦替代** | 防误操作/会话滥用，但硬门禁体验差 | 换软性摩擦（倒计时/风控/事后告警） |
| **参数放宽** | 阈值造成误伤 | 改参数 |
| **无关防护** | 无人使用、场景不存在 | 删除 |
| **告警收敛** | 通知价值 < 打扰成本 | 并入摘要或删除 |

**红线清单（不许动）**——因规模百人以上，比 v1 更宽：
①账号凭据 AES-GCM 静态加密（`account_crypto.py`）；②`.env` 窄行写入防行注入（`env_io.py`）；
③CSRF + 防 session 固定；④SSRF 白名单（`notify/config.py:91-140`）；⑤last-admin 防自锁；
⑥易班锁号冷却（保护真实账号）；⑦凭据熔断 + 分级重试预算；⑧**审计链 HMAC + 锚点 + audit_verify**
（用户明确要留：审计取证校验）；⑨**session sid 吊销/轮换**（用户明确要留：改密即踢旧登录）；
⑩tracking 加盐哈希（百人级真实手机号/IP 的隐私保护，与审计配套）；⑪注册/登录/IDOR 防护全套
（多用户防滥用是真需求，巡检证实现有限速有效拦截且不影响自然增长）；⑫时延拉平防枚举（微小，留）。

---

## 1. 放宽安全验证（治 UX 摩擦）

### 1.1 口令复核门 → 软性摩擦替代（本计划最大 UX 收益）

**现状**：13 类操作要求当次输管理员口令（scrypt 比对）：改他人凭据、purge/彻底删除、重置他人口令、
改角色、删用户、关通道、改 SMTP/收件人/推送密钥、改设置 A/B 档、发公告、执行体写、急停。
另有豁免 TTL 300s、复核冷却 300s、失败计数告警。

**用户确认**：此项碍事，勾选大幅放宽。设计输入：「走风控、后台验证、靠加载动画拉长时间等操控来减少风险」。

**改法（软性摩擦三件套替代口令输入）**：
1. **倒计时确认**（替代输入口令）：危险操作前端弹「此操作不可逆，N 秒后可确认」倒计时确认框
   （加载动画拉长时间的落地形态），后端要求请求带 `confirm_delay_ack` 且距上次危险操作 ≥ 间隔——
   防手滑连点，不打断心流；
2. **事后告警**（替代事中口令）：改他人凭据、purge 两类最高危操作保留**异步告警**
   （一封「刚才执行了 XX 操作」的通知，事后可追溯可回滚）；
3. **风控式加验**（替代一律加验）：仅异常态升级——同 IP 短时多次危险操作、非常用设备、
   操作频率超常时才要求口令复核（`YIBAN_PW_GATE=risk`，**建议默认**）。

档位：`off`（全软摩擦）/ `risk`（**默认**，异常才要口令）/ `full`（现状，保留兼容）。
连带：豁免 TTL/复核冷却/门禁失败计数告警仅在 `full` 下启用。

**测试影响**：`test_sensitive_gate_94`、`test_web_security_gates`、`test_switch_password_gate`、
`test_executor_write_guard`、`test_settings_tiers_94`、`test_credential_write_gate_94`、
`test_account_abuse_gate`、`test_role_hardening_0905`——按档位语义重写。

### 1.2 手动签到冷却 1800s → 同号 60s + 全局 10 次/600s（用户已拍板）

**现状**：`YIBAN_BATCH_SIGN_COOLDOWN_SEC` 默认 1800s 全局共用（`signin_api.py:161,260`）。
**改法**：默认 60s（与 `SIGN_MIN_INTERVAL` 对齐），加全局速率上限 10 次/600s 可配（防被盗会话刷真实登录）。
**测试影响**：`test_batch_sign_cooldown`、`test_batch18_knife2_0905`。

### 1.3 登录失败锁定放宽（用户确认碍事）

**现状**：同 (IP,用户名) 失败 5 次锁 300s（`auth.py:156-176`）。
**改法**：改 10 次→锁 60s（防暴力破解仍有效——巡检里真实攻击主要靠 nginx 边缘限速拦）。
登录频率 10/60s 保留。

### 1.4 高危操作额度放宽

5 次/60s → 20 次/60s（`app.py:624`）。软摩擦化后此项只是防脚本滥用的保险丝。

### 1.5 告警邮件（用户确认「刷屏」，全删管理操作类）

见 §3。

---

## 2. 缩减无关安全功能（v2 大幅收缩——规模校准后多数保留）

**v1 曾列的删除项经用户取舍后处置**（百人以上规模 + 用户明确取舍）：

| 项 | v1 计划 | v2 定案 | 理由 |
|----|---------|---------|------|
| 审计链 HMAC+锚点+audit_verify（1,300 行） | 降级普通日志 | **保留原样** | 用户勾选「审计取证校验」要留；百人级用户纠纷/滥用取证是真需求 |
| tracking 加盐哈希（133 行） | 删 | **保留** | 真实手机号/IP 隐私保护，与审计配套 |
| session sid 吊销/轮换（150 行） | 删 | **保留** | 用户勾选「改密即踢旧登录」 |
| 时延拉平防枚举 | 删 | **保留** | 百人级用户枚举有一定风险，成本为零 |
| **rekey 密钥轮换工具链**（720 行工具 + 2,871 行测试） | 移入 attic | **删除/attic**（用户未勾选） | 十年一遇场景，git 历史可考 |
| **clock_guard 时钟守卫**（约 300 行状态机） | 简化 | **简化**（用户「不清楚有没有可能触发，没见过」）：保留跳变告警 + 跳过清理一轮（几行判断），删持久化冻结与 `clock_guard_reset.py`。**注**：原文此处还列了"欠账追踪"，实为误列——全仓唯一的「欠账」是审计写入失败账目（`audit_chain.py` 的 `audit_write_fail_total`），属**红线⑧**，与 clock_guard 无关，**不得删** | 从未触发过，但防误删留个保险丝 |

**净缩减**：约 3,600 行（rekey 为主）+ clock_guard 状态机。比 v1 的 5,000+ 少——这是规模校准的正确代价。

---

## 3. 减少告警邮件（用户确认「刷屏」）

**现状**：几乎每个管理操作都发信（改设置/删用户/改角色/软删/注册/通道变更/摘收件人各有告警），
一天 5~10 封。另有每日通道健康日报。

| 事件类 | 现状 | 改后 |
|--------|------|------|
| 管理操作告警（改设置/删用户/改角色/软删/注册/通道变更/收件人摘除） | 逐条即时 | **删除**（日志页可查；§1.1 的事后告警只留最高危两类） |
| 登录失败告警 | 3 次即发 | 阈值 3→10（巡检证实 09-22 一天 9 封全是用户自己改密码的误报） |
| 高危口令门失败告警 | 即发 | 随 §1.1 档位，非 `full` 不发 |
| 签到失败（管理员汇总） | 收尾一封 | **保留**（核心运行报告） |
| 单账号耗时/容量超载/探针预警/窗口配置异常 | 各自独立成封 | **并入**收尾汇总信正文段落 |
| 用户签到失败信 / 审核拒绝信 | 即发 | **保留**（用户价值） |
| 通道健康日报 | 每日 | **改周报 + 降级即时** |
| 推送额度耗尽补发 | 即发 | 并入周报 |
| **备份失败哨兵（新增，巡检 B4）** | **无**（静默 4 天） | **新增**：每日 08:05 检查当日包存在，缺失→发一封告警（有节流）——这是「该有声音的故障」，与删噪音不矛盾 |
| SMTP 失败降级 webhook | urgent+force | 保留（真故障必须送达） |

**连带**：notify 三本账简化为一本 + urgent 标记；`urgent_only` 默认 0→1；巡检 B4 的 root crontab
MAILTO/备份行失败发声一并做。

**测试影响**：`test_alert_channel_dispatch`、`test_mail_notify`、`test_mail_failover_0907`、
`test_soft_delete_alert_0910`、`test_notify_throttle`、`test_notify_ledger_*`、`test_account_abuse_gate`。

---

## 4. 简化功能体系

### 4.1 设置 A/B 档合并
保留「仅主管理员」权限判定（A 档键仍限主管理员），口令要求统一随 `YIBAN_PW_GATE` 走；
急停双角色语义保留（安全件）。前端控件禁用逻辑随之简化。

### 4.2 在线验证体系——v2 改为保留
**v1 曾拟删异步任务体系，规模校准后推翻**：生产「账号提交即时验证已开启」、百人级用户持续
添加账号，验证是活功能；且「后台验证」正是用户软性摩擦思路的一部分（后台慢慢验，不阻塞提交）。
只做小幅精简：无人走的 cancelled/超龄收口分支合并。

### 4.3 配置面与文档瘦身
删掉的告警类对应配置项标注废弃；`docs/dev/` 相关文档同步。

---

## 5. 门禁口径变更声明（治理性）

PROMPT.md §6.10「安全测试通过」的口径变化比 v1 小得多：红线由七项扩为**十二项**（§0），
被删功能仅 rekey 与 clock_guard 状态机两簇，对应测试随功能走。实施批 commit 消息与记忆点文稿
逐条登记「删了哪些锁」。

---

## 6. 生产备份故障处置记录（2026-09-23 已完成）

巡检（`~/Desktop/生产日志巡检-2026-09-23.md`）B1/B2 根因：cron 调用的 `/usr/local/sbin/yiban-backup.sh`
是 09-05 旧拷贝（26,488B），`--require-encrypt` 与 `REMOTE_BACKUP` 绑死，09-20 起连续 4 晚
fail-closed 无告警；旧拷贝归档还不含状态文件与审计锚（B2）。

**已执行修复（用户授权「择机修复」，2026-09-23 12:49）**：
1. `cp -a` 留底旧拷贝 → `install -m 700` 仓库版（40,777B）覆盖运行版；
2. 手工补跑 `yiban-backup-wrapper.sh --require-encrypt` → 出包 `yiban-2026-09-23.tar.gz.gpg`（474K）+ `.sha256`；
3. B2 验证：归档内含 `sched-run-*`、`audit-anchor.log`（旧包缺的两样都在）；
4. 恢复演练 `--restore` 全绿：链自洽 + 锚点一致 + 无写入欠账，integrity_check 通过。

**遗留（并入 S1）**：①B4 哨兵（§3 新增行）防再次静默；②「运行脚本=仓库脚本」防漂移约束
（版本戳或 `diff -q` 自检，随 S1c 做）。

**巡检其余项对接**：**B3 不是缺陷（2026-09-23 复核订正）**——07:12 那条 cron 与首轮撞锁是
**预期现象**：补签已由 `run.sh` 的**进程内第二轮**承担（2026-09-10 批次20 方案一，判定收敛在
`state_io.need_second_run`），07:12 cron 是首轮被 `timeout` 击杀时的安全网（`run.sh:62-78`）；
生产 09-19 日志实证该轮会等到 07:12 执行（`=== 开始补签轮（第二轮）===`）。
**可做的只剩可观测性优化**（把这条重复触发挪到主跑之后或去掉，避免日志读起来像「没跑」）→ 归 S1c
与 B6 同批；而「量涨后主跑吃满窗口」的容量问题归调度线（M1 的 T9/T10：持久化队列 + 补签＝再消费
一遍队列 + 日终封账，消掉「哪一轮是最后一轮」的歧义）；B5（nginx `bad key share` 噪音）→ P3 顺手判定（10 分钟 IP/UA
回查，先判「影响真人/仅噪声」再决定动不动 TLS）；B6（排程/手工日志混写、缺退出码行）→ 并入 S1c
日志收敛（`trap EXIT` 必落退出码，入口前缀区分）。

---

## 7. 实施批次（v2 修订）与排期

**排期**：M1 合入 develop 后、M2 启动前插 S1（维持 v1 结论）；S1 独立分支 `feature/security-slim`。

| 序 | 内容 | 依赖 |
|----|------|------|
| S1a | §1.1 软性摩擦三件套 + `YIBAN_PW_GATE` 三档 + §4.1 档位合并 + 测试重写 | 无 |
| S1b | §1.2 签到冷却 + §1.3 登录锁定 + §1.4 额度 | 无 |
| S1c | §3 告警收敛 + 备份哨兵（B4）+ 防漂移自检 + §B6 日志退出码 + 周报化 | 无 |
| S1d | §2 rekey 退役 + clock_guard 简化（备份脚本对 audit 链的引用核对一遍——B2 验证已过，防回归） | S1c |
| S1e | §4.2 验证分支精简 + §4.3 配置/文档 + B5 噪音判定 | 无 |

S1b/S1e 可与 S1a 并行派发（不同文件域），里程碑提交制逐个落账。

## 8. 已定案记录（用户 2026-09-23 四问四答）

1. **规模**：百人以上（89 账号/110 用户，日增 20+）→ 红线扩容、§2 大幅收缩、验证体系保留；
2. **想留**：审计取证校验 ✅、改密即踢旧登录 ✅；时钟守卫「不清楚、没见过」→ 简化版（告警+暂停清理一轮）；
   rekey 未勾选 → 删；
3. **碍事**：手动签到冷却 ✅、登录锁定 ✅、危险操作口令 ✅、告警刷屏 ✅（四项全勾，均大幅放宽）；
   设计输入：软性摩擦（风控/后台验证/加载动画拖延）替代硬门禁 → §1.1 三件套；
4. **备份**：授权上服务器自查并择机修复 → B1+B2 已修复并验收（§6）。

---

## 9. 实施记录（2026-09-24）

**批次与提交**（分支 `feature/security-slim`，自 `develop @ 8fee031` 切出；三批各由一组实施者完成，A 批另过**一次对抗性独立复核**与**一次修复轮**）：

| 批 | 内容 | 提交 |
|----|------|------|
| **A 口令门** | `YIBAN_PW_GATE` 三档（`off` / `risk` 默认 / `full`）+ 软摩擦三件套（倒计时确认、事后告警、按需补凭据的提交入口）+ §4.1 档位合并 | 后端 `f654f3a` `269460b` `8211e19`；前端 `b0e64f0` `bb59658` `c518ef7`；返工 `aa54eb7` `9ae9232` `daaf0fd`；修复轮 `8522de3` `80788a6` `da487a5` `7107c3b` |
| **B 参数与告警** | §1.2 冷却 1800→60 + 全局速率上限 10/600s；§1.3 锁定 5→10、300→60；§1.4 额度 5→20；§3 告警收敛 + 备份哨兵 + 防漂移自检 + `run.sh` 退出码与触发来源 + §4.3 文档 | `8502cc1` `f061494` `96a4beb` `65cbce0` `dd633a4` `e4ab3a8` `4e6d3a9` |
| **C 退役与简化** | §2 rekey 工具链退役、clock_guard 改为"跳变只跳一轮"、通道健康日报改周报 | `43697fa` `d8eb6a7` `0234224` |

**门禁口径变更登记**（供 `PROMPT.md` §6.10「安全测试通过」核对）：

- **删除的外发告警 15 处**：改设置（含急停 force）｜彻底清除已注销用户｜批量删用户｜批量重置口令｜改角色｜单条重置他人口令｜完全删除用户｜批量彻底删账号｜批量软删账号｜单条软删账号｜单条彻底删账号｜邮件通知开关变更｜邮件 SMTP 配置变更｜告警收件人变更（含直发旧收件人）｜消息推送配置变更。**另**：时钟跳变日报（C 批，冻结状态播报随 clock_guard 简化下线）、单账号耗时与容量超载的即时推送（保留汇总段落）、`send_notification` 内联的推送额度耗尽补发（保留周报路径）。
- **放宽的阈值**：签到冷却 1800→60（同时**新增** 10 次/600s 全局上限，净效果是"防连点更紧、防误伤更松"）｜登录失败锁定 5→10 次、300→60 秒｜高危操作额度 5→20｜登录失败告警 3→10｜`urgent_only` 默认 0→1。
- **删除的功能簇**：rekey 密钥轮换工具链（720 行工具 + 专属用例）｜clock_guard 持久化冻结与人工重置工具（145 行）。
- **新增的机制**：`YIBAN_PW_GATE` 三档 + 不可逆操作倒计时确认 + 按需补凭据的统一提交入口｜备份失败哨兵 + 运行脚本防漂移自检（哨兵的部署前提是 **cwd 落在 `APP_DIR`**，由包装脚本自己 `cd` 保证：cron 的 cwd 是 `$HOME`，而哨兵的 `.env` 回落以 cwd 为基准——不 `cd` 就读不到 `.env`，收件人解析为空、告警发不出去，并在 `$HOME` 落一份野 `yiban.db`）｜`run.sh` 退出码留痕与触发来源前缀。

**与计划书的有意偏离（已核实，非疏漏）**：

1. **§3「高危口令门失败告警非 full 不发」→ 收紧为"任何实际要求了口令并失败的路径都发"**：`risk` 档只在换环境命中后才要求口令，那条路径上的失败正是攻击信号，静音它等于丢掉唯一的告警。
2. **`risk` 档判据换成"换环境"**（原设计"300s 内 ≥3 次危险操作"）：与既有 60s 额度重复，且会误伤"连续清理若干账号"这类最常见的合法操作。判据分析见 `.superpowers/sdd/security-slim/s1-plan.md §7`。
3. **§4.2 在线验证体系的分支精简：不做**。`cancel()`（仅 pending→cancelled）与 `reclaim_stale()`（CAS 拒绝）各有语义与用例，"合并分支"是纯外观收益而触碰状态机有真实风险。
4. **notify 三本账简化为一本：不做**。三本账各有独立日额度与测试，合并会改变限流语义；而噪音问题已由删除 12 处告警调用点解决。

**登记项（待处置，勿丢）**：

| 编号 | 事项 | 去向 |
|------|------|------|
| SL-1 | **急停（全停）的即时告警随 `api_settings_save` 尾部一起下线**：现在"立刻叫醒"只剩审计行。若认为安全件该保留声音，恢复一条独立告警即可 | 待裁决 |
| SL-2 | **`LOGIN_FAIL_NOTIFY` 被门禁失败告警与冷却布防起点复用**，阈值 3→10 顺带把"每窗口可做的 scrypt 尝试"从 3 提到 10。建议给门禁侧另立常量（保持 3） | **已修**（`2135739`，门禁侧另立 `SENSITIVE_PW_FAIL_NOTIFY=3`） |
| SL-3 | **跨用户名喷洒判据可达性待核**：实施者称"每 IP 登录频率 10/60s ⇒ 12 次请求的喷洒不可达"；按滑动窗口应可达。需实测确认，否则"喷洒→紧急"分支等于静音 | 下一批 |
| SL-4 | **`gated()` 的非取消失败分支同样不重载视图**（多段提交可能"已改一半而页面不刷新"）；取消路径已在 A 批修复 | **已修**（`39ce795`） |
| SL-5 | **执行体保存仍是两段请求**（行写 + 整条写），无法原子；取消/失败可能留下部分写入（现已重载视图并明示"部分修改已提交"）。根治需后端提供单请求原子接口 | 后续批 |
| SL-6 | **B5（nginx `bad key share` 噪音）**：需生产只读回查 IP/UA，判"影响真人 / 仅噪声" | 收口批（只读生产） |
| SL-7 | **`settings-mail.js` 成功提示只修一半**（`clearAdminTo` 仍被 `load()` 抹掉），两处口径不一致 | **已修**（`8e833dc`） |
| SL-8 | **`full` 档下删执行体行 / 清除出口变成"确认框 + 口令框"两个串行框**（只影响非默认档） | 登记 |
| SL-9 | 生产库 `app_meta` 的 `*_clock` / `clock_guard_alert` 行自本批起不再被读；**无害，不写迁移、不清理** | 登记 |
| SL-10 | `tests/test_sign_round_guards.py::BatchSignCooldownTest::test_cooldown_zero_disables` 在合并后的一次全量里红过一次；单文件串行与并行 3 次复跑全绿 ⇒ 负载敏感间歇项，与既有 `test_cooldown_blocks_immediate_retry` 同类 | 登记（M2 审查流"测试质量"维度） |
| SL-11 | **clock_guard"只跳一轮"的诚实边界**：正向拨快被拦后参照点落在被拨后的时间，若此后被 NTP 校正回真实时间会再触发一次回拨跳变 ⇒ **最多连跳两轮**（已在 `_CLOCK_ALLOW_FWD_HOURS` 注释说明） | 登记 |
| SL-12 | **时钟守卫注释与调用方实际行为不符**：`yiban/store/db.py` 的 `_clock_jump_guard` 原称"调用方在 `ok=False` 分支上会 rollback"，而 `yiban/store/users.py` 两处钩子是直接 `return`（无 rollback）。注释已订正为"守卫自己在越界分支提交参照点，故调用方是否 rollback 都不影响" | 已修（注释订正） |
| SL-13 | **`run.sh` 触发来源判据 `[ -t 1 ] \|\| [ -t 2 ]` 把"手工执行但重定向了输出"误判成排程**（手工跑 `run.sh > log` 时 stdout/stderr 都不是终端）；已改用 `[ -t 0 ]`（stdin 是否有控制终端），并在 `tests/test_run_sh_logging.py` 的用例里显式钉 `stdin=DEVNULL`，免得"用例宿主自身的终端"影响判据 | 已修（判据改 `-t 0`） |
| SL-14 | **`urgent_only` 默认 0→1 后，未置 `urgent=True` 的告警从"邮件+手机推送"变为"仅邮件"**：`web/routes/me.py` 三处失败告警（改密 / 注销 / 恢复）与注册用户自助改密的账号安全事件告警、`web/routes/my.py` 的新账号申请待审核、`web/routes/settings_api.py` 的公告草稿变更告警。**选择如实枚举而非补 `urgent=True`**：补 `urgent=True` 会与 `yiban/notify/config.py` 里"日常改密类只走邮件"的设计口径相反，且把高频信号塞回手机推送额度——与本批"收敛噪音"的目标冲突。已写进 §9 与 README「告警面」段 | 登记（文档已如实枚举） |
| SL-15 | **`--dist loadfile` 全量门禁并非确定性绿**：除 SL-10 外，`tests/test_login_e2e_mock.py::LegacyLoginE2ETest::test_legacy_chain_rehearsal` 与 `tests/test_verify_jobs.py::VerifyJobIntegrationTest::test_admin_submit_creates_active_account_rejected_on_failure` 也各红过一次（单文件复跑全绿）⇒ 同为负载敏感间歇项 | 登记（M2 审查流"测试质量"维度） |
| SL-16 | **`docs/dev/test-inventory.md` 头部计数失真**：文件写 126 文件 / 2664 用例，实际 137 / 2922（`ast` 口径）。该文件声称"自动生成"，但**仓库里没有生成脚本**（只有一处文档引用它），故只能手工刷新；已订正头部计数并注明分组表是旧快照。要根治得补生成脚本 + 门禁校验 | 登记（计数已订正） |
