# M3 修复流 · 批次 1 执行计划（红线批 + 测试地基）

> **是什么**：M3 修复流批次 1 的任务分解与执行纪律（测试地基 + 红线修复，SDD 驱动）。
> **状态**：定稿（2026-09-26，用户授权自主执行）。
> **范围**：覆盖 repair/m3-batch1 全部 14 任务与收尾链；不覆盖批 2+（隐私/告警/容量/CLI）与 MF-41 push 线。
> **读者**：批 1 各任务实现者与评审者必读；收尾链指挥者选读 §执行顺序与节奏。
> **基线**：develop `078720b`（MF-1..108）；分支 `repair/m3-batch1`（复用 `D:/code/yiban-wt-m3` worktree）。

> 目的：按 `docs/dev/must-fix-list.md`（develop@078720b，MF-1..108）的修复顺序，
> 本批做两段——**第 0 段：测试与 CI 地基**（用户 2026-09-25/26 指令：剔除最无用 20-40% 测试、
> 用好 GitHub CI、测试压进 3 分钟、给本机减负；含 MF-108 日期边界族修复）；
> **第 1 段：红线**（修复顺序第 2/3 组：审计可被抹、互斥可被绕、零守卫清库、补签链、
> 急停成环，外加 v3 灰度两条硬前置）。
> 执行方式：子代理驱动（SDD）——每任务一个实现子代理 + 任务评审（规格+质量）+
> 修复循环（≤5 轮）+ 全分支终审；分支收尾再跑一次隔离演练推演（按批 0 预案，裸机形态）。

## Global Constraints（每个任务的实现者与评审者都受其绑定）

1. **工作区**：只在 `D:/code/yiban-wt-m3`（分支 `repair/m3-batch1`）内改文件；不碰主工作区
   `D:/code/yiban-auto-sign`（登记册镜像除外，由指挥者同步）、不碰其他 worktree、不碰 `D:/code/yiban-wt-study`。
2. **git 纪律**：git 在 Git Bash 跑；**分支内禁止 push**（合并与推送由指挥者在用户授权下执行）；
   禁止 `git stash pop/apply`；禁止 `git add -A`（0 字节 `nul` 文件保持未跟踪）；逐文件 add。
3. **测试纪律（批 1 起改写，用户 2026-09-25/26 指令生效）**：
   - **本机只跑触碰面聚焦用例**（受影响文件 + 直接相关门子集）；全量交 CI nightly 轨；
     任务评审时指挥者本机跑一次全量作回归参考（裁剪后 ≈60-80s）。
   - pytest 与 ruff 仍在 WSL：`wsl -e bash -c "cd /mnt/d/code/yiban-wt-m3 && ~/.venv-yiban-wsl/bin/python -m pytest <目标> -q"`；
     ruff 命令与 ci.yml Lint 步骤逐字同口径（含 `web/`）。
   - **e2e > 套件**：行为变更导致既有测试红 ⇒ 报告/台账登记一行即可，不要求修绿；e2e 为准。
   - **TDD**：新行为先写测试再写代码（实现者报告须带 RED→GREEN 证据）。
   - 全量基线（批 0 合并后实测）：本机北京时区 3041 passed / 98s；MF-108 修复后增判
     **`TZ=UTC` 全量 0 失败**。已知干扰族（MF-37/38/107）按串行 + 同文件复跑口径裁定。
4. **MF-91 元纪律**：凡验收不变量是"判据/闸门"，修复必须附带至少一条**活体反例**测试——
   把输入改坏 ⇒ 判据必须红。没有反例的闸门测试视为未完成。
5. **行为契约不得破坏**：`run.sh` rc 契约（0/1/2/3）、`yiban/cli` 0/1/2/3/10、backup.sh
   0/1/4/5/6/7/8、pull-prod rc3、migrations EXIT_SCHEMA_MIGRATION=4；`.env` 既有键语义不变；
   引擎代理出口（`yiban/engine/egress.py`）行为不变。
6. **v3 灰度前置纪律**：登记表"v3 灰度批两个硬前置"（= 本批 Task 3+4 的两池互斥、Task 10 的
   v3 回炉口）完成并验证前，`SCHEDULER_V3=1` + `FALLBACK_ENABLE=1` 组合不得出现在任何
   配置示例、文档"推荐"或部署清单里。
7. **豁免设计保持（待裁决 #8，2026-09-25）**：手动签到不受急停/周末门影响是**写进 UI 契约的
   设计**——相关任务只改"显示与真值同源"与计数，不改门本身。
8. **测试卫生**：禁止"断言源码含某段字"的假绿测试；改行为同批更新钉旧行为的测试
   （或按本约束第 3 条登记无视）；测试输出干净。
9. **脱敏**：报告、日志摘录、测试里手机号一律 `138****0000` 形态；不引入真实凭据值。
10. **改动保守**：登记表行号可能偏移——按符号（函数名/变量名）定位；不顺手重构任务范围外代码。
11. **注释纪律（用户 2026-09-26 指令，强制）**：develop 已融入 M2 注释整备流产物，本批**新写/修改的生产代码
    （`yiban/`、`web/`、`scripts/`、`run.sh`、`docker/`、`deploy/`，测试程序豁免）必须符合
    `docs/dev/python-script-contract.md`**——新模块头部"四问"（功能/归属/复用/通信）；函数注释只讲
    "为什么/原理"不复述代码；**注释里禁任务痕迹**（批次号、评审轮次、修复日期、"修掉了什么"叙事、
    MF 号一律写在提交信息里，不写进代码注释）；必须保留的是约束说明、外部契约、踩坑警告。
    新增 `docs/**` 文档必须符合 `docs/dev/doc-writing-contract.md` 三段式（头部身份条五字段 + 速览 +
    目录契约 + 尾部缺口层）。实现者提交前自查、评审者按本条验收。

---

## 第 0 段 · 测试与 CI 地基（用户新指令，先行）

### Task 0 — MF-108 日期边界族锚定（S）

- **条目**：MF-108（CI 36 败实证，`TZ=UTC` 抽样 4/4 复现）。
- **改动面**：CI 失败清单 36 条对应的 ~16 个测试文件；再 `grep -rnE "datetime\.now\(|date\.today\(|utcnow\(" tests/`
  清扫潜伏同族（字面 `2026-` 日期一并过一遍）。凡涉"当日"语义一律锚到 `yiban.clock`（或显式 freeze）。
- **验收**：`TZ=UTC` 全量 0 失败（WSL 实测）；grep 判据复核；北京时区全量不退化。
- **e2e**：修后真实 push 一次（可在错位窗口内），CI 全量绿即最终证据。

### Task 1 — CI 双轨改造（M）

- **条目**：用户指令"合理利用 GitHub CI、测试压进 3 分钟内"+ MF-106 遗留。
- **改动面**（`.github/workflows/ci.yml` 拆两轨）：
  - **fast 轨**（push/PR 触发）：Lint（ruff，逐字同口径）+ 安全子集
    （`pytest tests/ -q -n auto -k "security or mask or audit or login or private or csrf or ratelimit"`，
    即发布门 §2① 命令）+ e2e smoke（挑 1-2 只全链用例，如 `test_login_e2e_mock`）。目标 **≤3min 含 setup**。
  - **nightly 全量轨**：`schedule: cron '0 6 * * *'`（14:00 北京，**避开 MF-108 错位窗口 UTC 16:00–24:00**）
    + `workflow_dispatch` 手动触发；全量 + `-p no:randomly` + xdist 调优（CI runner 4 核，`-n auto` vs `-n 8` 实测取优）。
- **判据**：fast 轨实测端到端时长（连续 3 次 ≤3min）；nightly 实测时长报备；连续 3 次 nightly 绿
  = "CI 基线可信"成立。若安全子集本身 >2min，缩减 `-k` 词集并报备（门子集以 fast 轨实际命令为准回写发布门文档）。
- **本机减负纪律（同步改写约束 3）**：本机日常 = 触碰面聚焦；安全门/全量门 = CI；SDD 实现"提交前全量"
  改为"触碰面聚焦 + 评审时一次全量参考"。
- **e2e**：真实 push 触发 fast 轨 + dispatch 触发 nightly 各实测一次，时长与结果写进任务报告。

### Task 2 — 测试瘦身 20-40%（L，两步走，清单先过目）

- **条目**：用户指令"剔除最没用的 20-40% 测试"；登记表 MF-14（源文本断言型占比过大）、
  MF-10/38/107（假绿/干扰/顺序依赖）为佐证。
- **2a 取证与清单**：全部测试文件归类计数（分类见下），产出剔除清单台账
  `docs/dev/test-slim-ledger-<日期>.md`（逐用例：文件::用例名 + 归类理由）。
  **清单产出后汇报用户过目，再执行 2b**（批内暂停点）。
- **2b 执行**：分文件剔除；每文件剔除后该文件聚焦跑；全量由 nightly 轨验证。
- **剔除资格（满足其一且不损失安全/e2e 覆盖）**：
  1. 源文本断言型：断言"源码文件含某字符串"而非行为（MF-14 家族）；
  2. mock 掉被测物本身的假绿（如 `set_session_cache` 整体 mock，MF-72 家族）；
  3. 同一分支多层重复：保留 e2e/集成层一条，砍重复单层；
  4. 已否证清单对应"防回归"用例（给否证结论守灵的）；
  5. 钉死旧行为且行为变更台账已登记"无视"的用例；
  6. 仅断言"调用发生过/计数相符"而无结果断言的用例。
- **保留禁区（0 删）**：安全/脱敏/鉴权子集、迁移链、rc 契约族、备份可恢复性、隔离护栏、
  e2e 全链（run.sh/mock）、CI 判据用例、SDD 反例用例。
- **深度**：20-40%（现约 3043 用例 + 1372 子测试 ⇒ 剔 600-1200 条）；实测后报备，红线 = 禁区 0 删。
- **e2e**：裁剪后 fast 轨 + nightly 各实测一次；夜里（错位窗口）push 一次验证 fast 轨稳绿。

---

## 第 1 段 · 红线（修复顺序第 2/3 组 + 灰度前置 + 演练带出项）

### Task 3 — MF-47 `(phone,day)` 互斥六条绕过路径（L，P0 地基）

- **改动面**（按登记表 ①..⑥）：① `reclaim=bool(args.only)` 的 done 行不校验 owner/租约即改回 claimed；
  ② `owner=excluded.owner` 短路租约判据、缺省身份 `single@{host}` 加 PID/代次；
  ③ **`claims.touch` 生产端接线**（执行侧周期心跳；"任何 >900s 仍 claimed 必须被 reaper 处理而不是被接管"）；
  ④ v3 `requeue_task` 的 WHERE 补 state+epoch；⑤ 运行锁三条 fail-open 收敛（超时放行/OSError→None/无 fcntl）
  + 四个调用点检查返回值；⑥ `claims.purge` 接时钟跳变守卫（同库其余 5 处已接）。
- **验收不变量**：领取"同事务内校验+写入"；心跳周期写并有断言；purge 走跳变守卫。
- **e2e**：双进程真领取互斥（沿用 V5/V6 临时库双进程法）；flock 无锁续跑注入 ⇒ 必须拒绝。

### Task 4 — MF-67 领取跨轮上限 + MF-74 `--only` 扇出收敛（M，依赖 Task 3）

- **MF-67**：`give_up` 按弃权原因分档——窗口外/无点位可当日再接手；预算耗尽/风控类需显式路径
  （同 `allow_settled` 一档）或按 `attempts` 列设当日上限。验收：`give_up` 后默认参数再领同一
  `(phone,day)` 必须 `ok=False`；补签/手动显式参数才 `ok=True`；`attempts` 递增可查。
- **MF-74**：`--only` 扇出收敛为单进程派发；三条确定性危害同批修——①抢输一路 rc=2 被写成
  "本轮未实际签到"的误报；②锁从"非阻塞即退 3"翻成 600s 阻塞后无锁继续（`workers.py:105` +
  `cli_support.py:112-134`）收敛；③ web `terminate()` 只杀监督进程的孤儿化。
  同批改：`runner.py:232-234` 注释、`tests/test_multi_executor_engine.py:289-331` 正向断言、
  `reviewfix-intended-design` L4-2 对齐说明。
- **验收不变量**：一条 `--only <单号>` 进程树整轮 `attempts.attempt_signin` 调用数 == 1、
  `Popen` 记录 ≤1、成功时 rc∈{0}，且 `--only` 仍能返回 3。
- **e2e**：`--only` 真跑进程树审计；双 worker 情形复跑确认不再扇出。

### Task 5 — MF-48 三处零守卫清库 + `--dry-run` 假演练（M）

- **改动面**：`generate_demo_data.py`（字面串比较守卫→目标指纹+确认+审计，删的含 `audit_logs`
  必须链校验显式异常）、`seed_accounts.py`（有清五表能力且零守卫、还改写生产 `.env`）、
  `state --yes`（无口令无审计、按文件名删 `db --backup` 副本）；`load_env_file:274` 静默继承宿主环境
  + `clear_session_cache:284` 无门 DELETE；包装层 `main(argv)` 死参数（`--dry-run` 静默丢弃真删 rc=0）；
  `backup.sh` 旗标只认 `${1}`。
- **验收不变量**：清库/删除类入口必须显式声明目标指纹（非路径字符串比较）+ 需要确认 + 入审计；
  被丢弃的参数必须报错；"dry-run" 三入口语义统一。
- **e2e**：`--dry-run` 真跑不删 + 真删路径缺确认被拒（两路都实跑）；参数丢弃场景 rc≠0。

### Task 6 — MF-52 审计锚点自检（M，红线）

- **改动面**：`_anchor_file_state` 补"变多"分支（仅追加 1 条垃圾行两道判据同时"无异常"）；
  非法 UTF-8 抛 `UnicodeDecodeError` 被 `web/app.py:2463` 吞成 WARNING ⇒ 改判"**无法定论**"
  （≠"无异常"）；校验器改取 `max_id` 最大真行；指纹跨权限存放（实现时给方案：至少"库内指纹 +
  另一属主文件指纹"双写，部署面改动进 manifest）。
- **验收不变量**：解析失败必须"无法定论"；指纹跨权限存放后，有 `yiban:yiban` 双写权限者
  不再能抹最近 N 条审计且 healthy=True。
- **e2e**：注入 1 条垃圾行 ⇒ 判据红（活体反例）；注入非法 UTF-8 ⇒ "无法定论"且有声音。

### Task 7 — MF-53 审计追责链（M，红线）

- **改动面**：业务写与审计写两个事务的中间被杀窗口（同事务或审计失败回滚业务）；无会话/请求 id；
  `audit_head_hash` 读失败与空链同返回 `""`；欠账单调无归零口径（urgent 永久刷屏）；
  `_rechain_audit_logs` 分批 commit 击穿原子承诺；`audit_verify.py` 无顶层异常兜底
  （`database is locked` 以 exit 1 冒充"检出篡改"；唯一下游"修复"是重跑 `migrate_v3` 会把可恢复态
  变永久断链；无锚点时把"没查"印成"通过"；删尾/整链重签检不出）。
- **验收不变量**：审计与业务同事务（或写审计失败即回滚业务）；"未查"与"通过"两个不同返回值；
  校验器对"锁住了"和"检出篡改"给不同退出码。
- **e2e**：锁库实跑校验器判码 ≠ 篡改判码；业务+审计间杀进程 ⇒ 欠账可检出。

### Task 8 — MF-46 `.env` 行模型统一（M，成环另一半）

- **改动面**：web（宽 `splitlines()` 10 分隔符）与引擎（只认 `\n\r`）两侧**一处行模型 + 一处校验器**
  （含 U+0085 等全部换行族）；`write_env_keys` 对传入 value/key 校验；注释里潜伏 U+0085 实体化攻击
  （V2 已活体复现：一次"只改签到模式"保存把 `YIBAN_GLOBAL_PAUSE=1` 实体化）⇒ 写入前后"键集合 diff"
  强制入审计，"本次未请求的键发生变化"必须写入失败；`paint()` 不静默换枚举不污染 `snap`；
  前端行分隔符校验。
- **验收不变量**：任意"本次未请求的键发生变化"必须让写入失败；含 8 个宽分隔符字符的入参两侧被同一句拒绝。
- **e2e**：V2 复现手法作活体反例（攻击路径必须被拒且留审计）；正常读写回归。

### Task 9 — MF-45 急停可见性 + MF-101 spawn 前过滤（S，面板假信号并单）

- **MF-45**：`sign-calendar-view.js:30-44` 漏 `global_paused`/`no_position`；日历图例只覆盖 12 状态里
  2 种 ⇒ 状态枚举与图例**同源**（一份表两侧消费）；三个窗口告警标记全仓无生产复位点 ⇒ 显式复位点+用例；
  web 侧 `day_off(env=None)` 落回 `os.environ` ⇒ 改读 `.env` 真值（**门本身不动**，约束 7）。
- **MF-101**：`signin_api.py:200/:294` spawn 前过滤 `user_paused`（批量计数虚高；只剩这一半，豁免设计保持）。
- **e2e**：急停置位后日历渲染 ≠"排队待签"；批量手动含自暂停账号 ⇒ 计数 == 实际 spawn 数。

### Task 10 — MF-43 补签链 ②→①→⑤（L；③ 撤除不做）＝ v3 灰度前置②

- **改动面**（顺序不可颠倒）：
  - **② rc 契约**：`workers._await_workers` 原始码（信号杀=负数）不得被 `run_worker_supervisor:172-179`
    归 0；任何非零/负数/信号不得写 SUCCESS；`SECOND_DONE_MARKER` 封存以"确实无未了结"为前置
    （含"未领取账号计入 `claims.stats` open/total"）；收尾标记由子执行体各写一份（`runner.py:609`）改单点。
  - **① 常驻兜底体**（`workers.py:184`）：每 60s 全量扫改真事件驱动；`:246` 让位分支只让"同一账号"
    不让"整段"（失败集中段 06:31–07:28 全程休眠的根）。
  - **⑤ v3 `sign_tasks.failed` 当日回炉口**（`claim_batch` 只取 pending ⇒ v3 失败无人回炉）
    ＝ **v3 灰度硬前置②**。
  - `--fallback` 在 `runner.py:185` 早于 `:462` v2/v3 分流 ⇒ 兜底硬编 v2 的分支修正；
    `YIBAN_FALLBACK_ENABLE` 缺省关 + 设键不拉进程 ⇒ 启动即告警。
  - **③ 撤除不做**：6 条前置 checklist 落文稿（07:12 固定轮是唯一"主进程消失后才动"的恢复腿，
    被 ② 的坏 rc 弹开才今天不触发）。
- **验收不变量**：登记表原文（非零/负数/信号不写 SUCCESS；封存前置；开关置 1 无进程启动告警）。
- **e2e**：真 kill 信号注入子执行体 ⇒ run.sh 不得写 SUCCESS；v3 注入失败任务 ⇒ 当日回炉领到。

### Task 11 — 假上游故障注入旋钮（M；收尾轮顺序 5 的修复流部分）

- **改动面**：`mock_env` 最小注入面四类旋钮——登录失败 / 提交失败 / 风控挑战页 / 非 JSON 响应
  （环境变量或 mock 场景声明）；`ensure_certs` 修 CA 证书 keyUsage+basicConstraints
  （**E1 勘误代码化**，S6b 已实测该修法全绿，Python ≥3.14 strict 校验下不再整轮全灭）。
  L3 剩余 16 格不在本批（容量批）。
- **e2e**：四类旋钮各一条从 `run.sh` 入口到 mock 记账的全链实跑；旋钮关闭时回归无影响。

### Task 12 — MF-71 WAF 分类 + MF-72 假成功（M，依赖 Task 11 旋钮）

- **MF-71**：`waf.py` 15 处 raise 逐条落**显式"不可重试"档**（现状全落 MAX_ATTEMPTS=3 普通重试档，
  既不清会话也不减速）；四份分类口径（`classify_failure`/`_retry_budget`/`probe.py PROBE_HARD_FAIL_RE`/
  登记表词元）收敛为一份；`is_waf_blocked` 的 `len>2000` 短路修 + **同批改**
  `tests/test_login_protocol_shape.py:617-622`（把旧行为当规格钉住处）；每档活体反例。
- **MF-72**：`login_killyiban` 唯一判据 `code==0` ⇒ 新增"签发方回执"级判据（**不得照抄**
  `login_legacy:359-360` 的近恒真 cookie 复核）；无条件 `session_store.save()` 改判据后；
  相关测试不再整体 mock `set_session_cache`。
- **e2e**：Task 11 旋钮注入"挑战页 / 非 JSON / 假 code=0"三类 ⇒ 日志分类落正确档、不写假成功、
  密文库无污染、探针口径预警。

### Task 13 — MF-75 容器调度器异常面 + run.sh LOCK_DIR（S，演练带出项）

- **MF-75**：`docker/scheduler.py:228 _run_signin_child` 的 `Popen` 补 `except OSError`
  （对齐同文件 `_start_fallback_child:299` 判法）；`supervisord.conf` 补 `startsecs/startretries`；
  `docker-compose.yml` healthcheck 补 sched 探活（现只 curl web 端口，restart 永不救）。
  （双跑/孤儿面归 MF-47 补句，本条只做异常面。）
- **run.sh LOCK_DIR**（批 0 演练带出）：主锁目录 `mkdir` 失败不再静默回退 `/tmp`（与 08-21 对抗性审查
  注释自相矛盾）——改 fail-closed 或要求显式 `YIBAN_LOCK_DIR`；回退分支 `mkdir` 结果必须检查。
- **e2e**：Popen 注入用例；`LOCK_DIR` 不可建 ⇒ 拒绝运行 rc=1；容器形态若本机 docker 可用则
  `docker build` + 单 tick 异常演练，不可用则按批 0 预案容器段标注待生产演练。

---

## 批次边界（本批不做）

- **MF-44 告警链**：修复顺序未点名（语义属"声音"族）——**已裁决（2026-09-26，用户授权自主决策）：归批 2 首条**（与 MF-90 告警幂等、隐私出口同批，告警族集中一处）。
- MF-49/50/51/58/59（隐私与凭据批，= 批 2 主体）；MF-54 口径总账贯穿。
- MF-56/57/55 容量口径 + L3 16 格（批 3）。
- MF-92..100 自检面、MF-102/103 低危尾批。
- MF-60/61 CLI/前端批。
- MF-43 **③ 撤除**（本批只落 6 条前置 checklist）；v3 灰度开关进生产（约束 6）。
- MF-41 push 与发布线统一（用户执行）；备份口令轮换（用户执行，7 步清单在 DISPATCH-REMAINING.md）。
- semgrep 6 硬门入库（待裁决 #10）；SL-* 项。

## 执行顺序与节奏

第 0 段串行（Task 0 → 1 → 2，其中 2a 后批内暂停一次等用户过目剔除清单）；
第 1 段 Task 3 → 4（依赖）→ 5..9 可按登记顺序串行 → 10 → 11 → 12（依赖 11）→ 13。
分支收尾：全分支终审 → 一轮修复 → 隔离演练推演（批 0 预案裸机形态，含本批新护栏）→ 合并 develop → push → 查 CI。

## 尾部 · 明确缺陷与待办

- **缺陷**：无（本文件为计划，执行缺陷记 SDD 台账 `.superpowers/sdd/m3-batch1-plan-20260925/progress.md`）。
- **待办**：push 后验证清单未闭环（fast 轨 ≤3min ×3、nightly dispatch 计时、连续 3 次 nightly 绿）——挂在分支收尾 push 后执行；Task 2a 剔除清单的 T3 档待指挥者+用户裁量。
