# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv/
.env

# 本地配置（避免泄露账号密码）
config.local.py
*.local
# 账号配置文件（含密码，严禁入库）
accounts.json

# SQLite 数据库（含账号密文/用户哈希/审计日志，严禁入库）
yiban.db
yiban.db-wal
yiban.db-shm

# IDE
.vscode/
.idea/
*.swp
*.swo

# 日志与数据
*.log
logs/
data/

# Docker 部署本地目录（含加密密钥/数据库/证书私钥，严禁入库）
certs/

# 部署打包文件（不应进仓库）
*.tar.gz
*.zip

# 临时提示词文件
PROMPT_FOR_MULTIMODAL_MODEL.txt

# 系统文件
.DS_Store
Thumbs.db

# 网页管理系统：普通用户表（含密码哈希）
users.json

# 本地调试目录（不提交）
demo-log/
demo-tmp/

# 备份目录（防历史敏感文件被误提交）
backup-*/
.backup-*/
# 2026-08-20 对抗性审查修复：复数形式 backups/ 此前漏网（内含 deploy-local
# 敏感文档副本与源码快照，一次 git add -A 即泄露）
backups/

# 第三方站点产物（与本项目无关，无来源记录不入库）
deepseek_platform.css
deepseek_platform.js

# 本地漏洞测试环境（隔离实例/模拟代理/模糊脚本，不入库）
test-tmp/

# 服务器访问信息（含真实 IP/手机号，严禁入库）
服务器访问信息.md

# 内部运维文档（含服务器 IP/部署配置/漏洞细节，本地保留不入库）
MAINTENANCE.md
PROXY_DEPLOY_GUIDE.md
ADVERSARIAL_REVIEW_20260820.md
hardening/
# docs 目录一律不跟踪（内部运维/设计/调研文档仅本地留存，2026-08-22 起整体移出版本控制）
docs/
.superpowers/

# ZCode 工具目录
.zcode/
.jspace/
.ruff_cache/
.pytest_cache/

# 内部 AI 工作记忆（含服务器 IP/部署细节，严禁入库，2026-08-15 对抗性审查 F1）
.workbuddy/

# 设计参考图（非产物，2026-08-15 对抗性审查 F2）
build/

# .env 并发写锁文件（2026-08-15 对抗性审查 B）
.env.lock

# 品牌图标（版权来源不明，不入开源仓库；本地/服务器自行放入即可生效，见 README「自定义 Web 图标」）
web/static/vendor/logo.png

# 运营者本地完整版合规文档（不入库，仅自己的服务器用；仓库内相应文件为只含注释的空模板）
deploy-local/

# 根目录 agent 记忆入口（2026-08-16 归并：AI-HANDOFF / TODO / .workbuddy 记忆 / WORKFLOW / CONTRIBUTING，非开源）
memory.md
AGENTS.md

# 协作备用文件（未启用；等协作场景再决定入库，2026-08-16 补忽略）
WORKFLOW.md
CONTRIBUTING.md
.github/pull_request_template.md
# 2026-08-22 M10：ci.yml / mirror.yml 解除忽略入库（文件内只有 secrets 名引用无值，
# 入库安全），并全量 SHA 固定第三方 action（防可变 tag 被篡改）
# signin.yml 必须入库：README「GitHub Actions 使用教程（备选）」的部署通道，
# Fork 用户依赖此文件；仓库所有者侧通过 Actions 页面 Disable 控制不跑（2026-08-22 复核恢复）
.commitlintrc.json
scripts/git-hooks/

# 本地维护工具（含演示凭据/依赖本地字体环境，仅本机使用，2026-08-22 移出版本控制）
scripts/init_demo_credentials.py
scripts/scan_ui_emoji.py

# 标题 Web 字体文件（阿里妈妈方圆体，免费商用但体积大不入库；部署时放置，
# 见 web/templates 标题 @font-face 与 AGENTS.md 字体小节，2026-08-22）
web/static/vendor/fonts/fangyuan/

# 本地工具配置目录（DSH 路由套件 / 模型 preset，非项目产物，2026-08-16 补忽略）
dsh-routing-suite/
v4-flash-godmode-opencode-go/
